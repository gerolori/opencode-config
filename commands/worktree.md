---
description: Start a task in a dedicated worktree
agent: plan
---

Load the worktree-flow skill if needed.
For task name "$ARGUMENTS", create the standard worktree and switch work to that path.
Then summarize the execution plan.
